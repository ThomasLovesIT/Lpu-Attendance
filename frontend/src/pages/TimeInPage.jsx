import React from 'react';
import Index from './Index'; // Import the new Stranger Things Layout

const TimeInPage = () => {
  // Render the Layout in "IN" mode
  return <Index type="IN" />;
};

export default TimeInPage;