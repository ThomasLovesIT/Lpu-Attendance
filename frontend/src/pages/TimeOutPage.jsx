import React from 'react';
import Index from './Index'; // Import the new Stranger Things Layout

const TimeOutPage = () => {
  // Render the Layout in "OUT" mode
  return <Index type="OUT" />;
};

export default TimeOutPage;